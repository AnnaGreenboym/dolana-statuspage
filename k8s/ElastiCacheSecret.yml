apiVersion: v1
kind: Secret
metadata:
  name: elasticache-secret
  namespace: status-page
type: Opaque
stringData:
  REDIS_HOST: your-elasticache-endpoint.redis.amazonaws.com
  REDIS_PORT: "6379"
  # Add any authentication details if needed
